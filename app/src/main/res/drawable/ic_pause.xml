<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="120dp"
    android:height="120dp"
    android:viewportWidth="@integer/viewport_width"
    android:viewportHeight="@integer/viewport_height">

    <group
        android:name="@string/ic_group_parts"
        android:pivotX="@integer/viewport_center_x"
        android:pivotY="@integer/viewport_center_y"
        >

        <group
            android:name="@string/ic_group_top">
            <path
                android:name="@string/ic_pause_pathname_top"
                android:pathData="@string/path_pause_top"
                android:fillColor="#ff000000"
                />
        </group>

        <group
            android:name="@string/ic_group_bottom">
            <path
                android:name="@string/ic_pause_pathname_bottom"
                android:pathData="@string/path_pause_bottom"
                android:fillColor="#ff000000"
                />
        </group>

    </group>

</vector>
